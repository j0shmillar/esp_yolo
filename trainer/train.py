# I want to train my own YOLOv1 model for object detection in tensorflow. dataset is under folder 'dataset/images' and 'dataset/labels'. labels are [class, x, y, w, h] in txt file. x,y, w, h are normalized by image width and height. No pre-trained model is used.  grid size is 4x4, 2 bounding boxes per grid cell, 4 classes.



